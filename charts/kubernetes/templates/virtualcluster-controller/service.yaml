apiVersion: v1
kind: Service
metadata:
  name: unikorn-virtualcluster-controller
  labels:
    {{- include "unikorn.labels" . | nindent 4 }}
spec:
  selector:
    app: unikorn-virtualcluster-controller
  ports:
  - name: prometheus
    port: 8080
    targetPort: prometheus
